Logging objects
Don't use console.log(obj), use console.log(JSON.parse(JSON.stringify(obj))).

Массив объектов это массив ссылок!


наконец-то я пришёл к тому, что моё расширение больше не нужно:
https://addons.opera.com/ru/extensions/details/copy-urls-2/
здесь есть хорошие фичи
https://addons.opera.com/ru/extensions/details/bulk-url-opener/
просто я не догадался написать url exporter


Замечание по структуре: выбранную сессию, вероятно, лучше хранить как свойство сессии (isSelected).


Текущая задача:
//-- сделать нормальные названия функций, занести imex в структуру. // сделал, вроде работает
- импорт в окне: сделать определение JSON это или нет. Учесть, что названия сессий могут начинаться с {[. Пока что пусть добавление текстом будет по одной сессии, надёжный парсер на схожесть с JSON делать лень. Сделать автодобавление протокола http:// если в ссылке его нет, как тут: https://addons.opera.com/ru/extensions/details/bulk-url-opener/

//- импорт: отделить первое число (индекс) от ссылки //сделал

- экспорт: в текст без мусора в названии (оставил заготовку)
- экспорт в текст без добавления в сессию

импорт - при проверке пересортировать по индексам, определить повторящиеся
//- импорт: Строка-не-URI как название сессии и выделение sID //вроде сделал

экспорт в файл

TODO:
Запили выбор вкладок для добавления: в попапе на первой вкладке выбираешь этот режим, запоминаешь это 
в бэкграунд процессе, заменяешь попап на сообщение "теперь выберите вторую вкладку и откройте расширение на ней". 
Переходим во вторую вкладку, там нажимаем попап, где есть кнопка "сохранить до сюда" и "отменить операцию".
Комментарии к вкладкам?
Сделать выбор вкладок в сессии шифтом:
Когда-нибудь допилить до принципа V7, пока что можно обойтись перемещением и копировать вкладки в другую сессию,
а так же их сравнением. Возможно это функционал для отдельной страницы, как и импорт экспорт.
Отмена действий.
Импорт из текста: сделать загрузку названий страниц (можно по кнопке)
- импорт из текста: учитывать индекс перед ссылкой

-----

v0.5 текущая
Сделал экспорт в попапе, начал делать импорт.
Импорт текста: анализ наличия JSON частично написан, но не работает хорошо 
Добавил alertResponse, но мб надо будет допиливать по надобности. Переделал кнопку удаления вкладки.
Оставил наработки по сортировке по указанному индексу в сессии.

v0.4.2
Разнёс большинство функций из actions.js по разным файлам, там остались только те, которые я ещё не определил куда поместить, css тоже разделил. Начал длеать экспорт/импорт -- видимо, требуется полная переделка как минимум генератора cодержимого tabs_list (listManipulatorTabs), а возможно придётся пересмотреть всю архитектуру. перекатываюсь на 0.5 

